Show the current development status of the opentomy project.

Steps:
1. Check which packages exist and have been implemented (check for src/index.ts or dist/):
   - packages/types
   - packages/crypto
   - packages/db
   - packages/ui
   - apps/web
   - apps/desktop
2. For each, print: exists / has source / has build output
3. Run `git status` to show uncommitted changes
4. Check if .env exists (not .env.example)
5. Check if node_modules exist at root
6. Print a checklist of what's done vs. what still needs work
